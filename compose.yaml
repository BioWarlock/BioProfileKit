services:
  bioprofilekit:
    image: bioprofilekit-tool:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./testdata:/data
    entrypoint: [ "bioprofilekit" ]
    command: ["bioprofilekit", "-i", "data/winequality-white.csv"]